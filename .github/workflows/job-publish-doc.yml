# Reusable workflow to publish documentation to GitHub Pages
name: Job - Publish to Pages
run-name: Publish to Pages

on:
  # Allows to be called by other workflows
  workflow_call:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    # This job is disabled by default, as you may not wish to publish the code documentation to GitHub Pages. If you do,
    # enable the project setting to publish to Pages through an action, then remove the following line
    if: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: psdi-uk-runners
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[gui-test,doc]
      - name: Build doc
        run: pdoc --html variability_plot
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          # Upload the directory generated by pdoc
          path: "./html/variability_plot"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
