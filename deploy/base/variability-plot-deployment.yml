apiVersion: apps/v1
kind: Deployment
metadata:
  name: variability-plot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: variability-plot
  template:
    metadata:
      labels:
        app: variability-plot
      annotations:
        elastic.co/dataset: kubernetes.container_logs.variability_plot
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - variability-plot
                topologyKey: "kubernetes.io/hostname"
      containers:
        - name: variability-plot
          image: variability-plot
          volumeMounts:
            - mountPath: /tmp
              name: uploads
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi" # this value may need to increase if application stability declines
              cpu: "1000m"
      imagePullSecrets:
        - name: github-psdi-registry-credentials
      volumes:
        - name: uploads
          emptyDir:
            sizeLimit: 1Gi
